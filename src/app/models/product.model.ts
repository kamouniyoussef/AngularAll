export interface ProductModel {
  id_product?: number;
 
   category: string;
  description: string;
  image_url: string;
  name: string;
  price: number;
  quantity: number;
  sku: string;
  unit: string;
} 